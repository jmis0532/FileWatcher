# FileWatcher 專案

## 專案目的
用 C++ 撰寫一支 Windows 視窗化（GUI）程式，**同時監控使用者指定的多個目錄**（每個目錄各自含所有子目錄），並把檔案清單記錄到文字檔，且設定一次後可開機自動在背景執行：

1. **第一次執行**：視窗顯示一份「監控資料夾」清單（ListView，欄位：資料夾路徑 / 狀態），預設先放入程式啟動時的工作目錄一筆，狀態「已停止」。
   - 按「新增資料夾...」可以加入更多資料夾一起監控；按「移除資料夾」可以把已停止監控的資料夾從清單移除（**監控中的資料夾不能移除**，要先停止）。
   - 在清單中選取一列，按「開始/停止監控」個別啟停該資料夾；每個資料夾各自跑一條獨立的背景監控執行緒（`MonitorEntry`），互不影響——可以同時有些資料夾在監控、有些已停止。
   - 只要有任何資料夾被「開始監控」過，程式就會把**目前整份資料夾清單**存進設定檔，並在登錄檔註冊開機自動啟動。
   - 每個資料夾各自：先掃描目錄（含所有子目錄），把「檔名（相對路徑）、容量、記錄時間」寫入該目錄底下**自己的** `file_list.txt`（這份清單是**永久累積的歷史紀錄**，不會在每次啟動時清空）；之後新檔案移入即時附加紀錄，同步顯示在視窗下方共用的「監控紀錄」欄位（每則訊息都含完整路徑，可以分辨是哪個資料夾的事件）。
2. **之後每次開機／登入**：Windows 自動啟動本程式，程式偵測到設定檔已存在，**不顯示視窗**，直接在背景把清單裡的**每一個**資料夾都開始監控，只在系統匣顯示一個圖示。
3. **重複檔案偵測**（每個資料夾各自獨立比對自己的歷史紀錄）：新檔案移入時，比對它的相對路徑是否在該資料夾的歷史紀錄中出現過（不管是這次 session、還是好幾年前的舊紀錄）：
   - **從未記錄過** → 直接記錄，不必理會，不會有任何提示。
   - **曾經記錄過** → 不會立刻寫入，而是先收集起來；同一批次（同一次系統通知事件）收集到的所有重複檔案，會合併成一個視窗一次列出（支援多檔案批次處理），使用者可以逐一勾選要「刪除」的檔案，未勾選的視為「繼續移入」，會保留並記錄一筆新紀錄。
4. 按視窗右上角 X 只是把視窗縮到系統匣，所有正在監控的資料夾會繼續在背景執行；系統匣圖示右鍵選單可「顯示視窗 / 開始監控全部 / 停止監控全部 / 結束程式」，雙擊圖示可叫回視窗。
5. **「匯出檔案清單」按鈕**：對清單中選取的資料夾，不需要先啟動監控，隨時可按，會手動觸發一次遞迴掃描（含所有子目錄／子子目錄），把「歷史紀錄裡還沒有的檔案」直接記錄進該資料夾自己的 `file_list.txt`（不會另外開新檔案）。

（2026-08-18：主控台版本 → 視窗化版本 → 開機自動背景執行版本 → 重複檔案偵測版本；2026-08-19：改寫為**多目錄監控**版本。完整演進與測試記錄見下方「執行狀態」。）

## 真實使用情境（為什麼需要這支程式）
使用者實際的使用流程是：從網路下載日文命名的檔案 → 把下載好的檔案移入這支程式監控的資料夾 → **移入之後**把檔案在資料夾裡改成中文檔名（方便自己整理／辨識）。

使用者擔心的問題：因為改成中文名稱後，資料夾裡已經看不出「這個檔案原本的日文檔名是什麼」，如果之後不小心又下載到同一個日文命名的檔案、又把它移入資料夾，會造成重複下載/重複整理。所以這支程式的核心目的就是：**用原始的日文檔名當作比對依據**，記住「這個日文檔名的檔案已經處理過」，下次同名的日文檔案再被移入時主動提示。

這個需求剛好完全對應「重複檔案偵測」功能的設計（見「已確認的需求 / 設計決策」）：
- 檔案以**原始檔名**（例如日文檔名）第一次移入時，就會被記錄進歷史紀錄。
- 使用者之後在資料夾裡把它**改名**成中文，這只是一筆新的紀錄（新的相對路徑），**不會**覆蓋或刪除原本日文檔名的歷史紀錄——所以歷史紀錄裡永遠留著「這個日文檔名曾經被處理過」的證據，不受後續改名影響。
- 之後只要有人（不管是使用者自己不小心，或任何來源）把**同一個日文檔名**的檔案移入監控資料夾，程式一定會比對到歷史紀錄裡的日文檔名，跳出重複檔案提示，讓使用者決定要刪除還是保留。
- 2026-08-19 已用真正的日文檔名（`テスト動画ファイル.mp4`，含平假名／片假名／漢字）實測驗證整個流程，確認檔名編碼、歷史比對、改名後歷史不消失、重複偵測都正確運作，詳見「執行狀態」。

## 專案資料夾
`D:\FileWatcher`

## 檔案清單
- `FileWatcher.cpp` — 主程式原始碼（單一檔案）。
- `file_list.txt` — 程式執行後，在**每一個**被監控的目標目錄底下各自產生的紀錄清單（不是放在專案資料夾裡，除非監控目標就是 `D:\FileWatcher` 本身；同時監控多個資料夾時，每個資料夾各有自己獨立的一份）。

## 已確認的需求 / 設計決策
- **目標平台**：Windows。使用 `ReadDirectoryChangesW` API 做即時監控（非輪詢），採 Overlapped I/O + 停止事件（`WaitForMultipleObjects`），讓監控可以隨時被使用者從畫面上按「停止監控」中斷。
- **介面**：Win32 原生視窗（不依賴 MFC/Qt 等額外框架，維持專案「靜態編譯、最少 DLL 依賴」的原則）。
  - 「監控資料夾」清單（`SysListView32`，report 模式，單選）：欄位為「資料夾路徑 / 狀態」，每一列對應一個 `MonitorEntry`。
  - 「新增資料夾...」：用 `SHBrowseForFolderW` 開啟標準 Windows 資料夾選擇對話框，選好後新增一列（狀態「已停止」），會檢查是否已存在於清單中（不分大小寫比對）避免重複加入。
  - 「移除資料夾」：只能移除「已停止」的項目；監控中的項目會跳出提示要求先停止。
  - 「開始/停止監控」：對清單中目前選取的那一列個別啟停（不是全域切換）；沒有選取任何列時會提示先選取或新增。
  - 「匯出檔案清單」：對清單中選取的那一列手動觸發一次掃描。
  - 監控紀錄欄位：唯讀多行文字方塊，所有資料夾共用同一個顯示區域，即時顯示掃描與新增檔案的訊息（每則訊息都含完整路徑可分辨來源資料夾）；背景執行緒透過 `PostMessage` 把訊息安全地送回主視窗執行緒顯示，避免跨執行緒直接操作控制項。
- **多目錄架構**：`MonitorEntry` 結構代表清單中的一個資料夾，內含穩定的整數 `id`（新增時遞增配發，不會因為清單增減而重複使用，避免 ListView 列與背景執行緒之間對應錯亂）、資料夾路徑、自己的 `HANDLE hStopEvent`、自己的 `std::thread`、自己的 `std::atomic<bool> isMonitoring`。所有項目存在 `std::vector<std::unique_ptr<MonitorEntry>> g_entries`（用 `unique_ptr` 是因為 `std::atomic`／`std::thread` 都不可複製或搬移，vector 增減元素時只搬動指標、不會搬動底層物件本身，執行緒與 atomic 的位址維持穩定）。ListView 每一列的 `lParam` 存對應的 entry id，需要時用 `FindEntryById` 依 id 反查（而不是用 vector 索引，因為移除項目後索引會位移）。
- **執行模式**：每個資料夾各自的監控邏輯跑在**獨立**的背景執行緒（`std::thread`，存在對應的 `MonitorEntry::thread`），彼此完全獨立、互不阻塞；UI 執行緒只負責訊息迴圈與畫面更新，不會被任何一個監控邏輯卡住。背景執行緒結束時透過 `WM_APP_MONITOR_STOPPED`（`wParam` = entry id）通知 UI 執行緒去 `join()` 該執行緒並更新對應列的狀態文字，若同時有多個資料夾在啟停，各自的完成通知不會互相干擾。
- **掃描範圍**：包含所有子目錄（遞迴監控與遞迴初始掃描）。
- **編碼**：清單檔案用 UTF-8（含 BOM），避免中文檔名亂碼。
- **新檔案判定**：處理 `FILE_ACTION_ADDED`（新增/從別處移入）與 `FILE_ACTION_RENAMED_NEW_NAME`（移動/改名進來）兩種事件。
- **紀錄格式**：Tab 分隔，欄位為「檔名(相對路徑) / 容量(bytes) / 記錄時間」。
- **設定持久化（多目錄）**：目前整份監控資料夾清單存在與 `FileWatcher.exe` 同資料夾的 `filewatcher_config.txt`（UTF-8 BOM，**一行一個資料夾路徑**，`SaveConfig(std::vector<std::wstring>)`／`LoadConfig()` 回傳 `std::vector`）。舊版單一資料夾格式（單行一個路徑）可以無痛升級相容，因為讀取邏輯本來就是逐行讀取，單行檔案自然就是「清單裡只有一個資料夾」。
  - 只要清單「內容」有變化（新增、移除資料夾）就會呼叫 `PersistFolderList()` 立刻存檔；按「開始監控」時也會存一次（確保開機自動啟動註冊時機正確）。
  - 程式啟動時載入設定檔，只保留「目前仍然有效」的路徑（`fs::exists` + `fs::is_directory` 檢查），失效的路徑會被靜默捨棄，並順便重新存檔把設定檔一併清乾淨；若清乾淨後一個有效資料夾都不剩，就當成「第一次執行」處理（顯示視窗、預設放入目前工作目錄一筆，不會自動背景啟動）。
- **開機自動啟動**：寫入登錄檔 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run`，值名稱 `FileWatcher`，內容為本程式的完整路徑（加雙引號）。這是「目前使用者」層級的自動啟動（不需要系統管理員權限），所有使用這台電腦同一個 Windows 帳號登入時都會生效。
- **背景執行 / 系統匣**：用 `Shell_NotifyIconW` 顯示系統匣圖示。視窗建立時預設不加 `WS_VISIBLE`；若偵測到有效設定檔就完全不呼叫 `ShowWindow`，改用 `WM_APP_AUTOSTART_ALL` 訊息通知 UI 執行緒對清單裡**每一個**資料夾呼叫 `StartMonitoringEntry`，全程沒有任何視窗或工作列項目出現，只有系統匣圖示。按視窗 X（`WM_CLOSE`）預設只是 `ShowWindow(SW_HIDE)`，所有監控中的資料夾都不受影響；系統匣選單改成「顯示視窗 / 開始監控全部 / 停止監控全部 / 結束程式」（因為多目錄之後不再有單一的「監控中/已停止」狀態可以做成單一切換項目，改成對清單裡所有項目一次套用）；只有「結束程式」（設定 `g_forceExit` 旗標）才會真正停止所有監控、移除系統匣圖示並結束程式——關閉前會先對所有監控中的項目送出停止訊號，再依序 `join()` 所有背景執行緒。
- **移除開機自動啟動的方式**（目前沒有內建 UI，需手動操作）：刪除登錄檔值 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run\FileWatcher`，並可選擇刪除 `filewatcher_config.txt`（下次執行就會恢復成「第一次執行」的正常視窗模式）。
- **介面字型**：Win32 的 `CreateWindowExW` 建立控制項時，如果沒有明確指定字型，預設會套用很舊、沒有反鋸齒的點陣字型（"System" font），在現代高解析度螢幕上看起來會很醜、有鋸齒。已修正為：程式啟動時用 `SystemParametersInfoW(SPI_GETNONCLIENTMETRICS, ...)` 取得 Windows 目前實際使用的訊息框字型（`lfMessageFont`，通常是 Segoe UI 或微軟正黑體之類，依系統語言而定），建立一個 `HFONT`，再對每個視窗控制項（含主視窗與重複檔案清單視窗的所有控制項）呼叫 `WM_SETFONT` 套用，讓介面文字跟 Windows 原生對話框一樣有平滑的反鋸齒效果。
- **「匯出檔案清單」按鈕**：對清單中目前選取的那一列（`GetSelectedEntry()`）觸發。點下去會在背景執行緒跑 `ExportWorkerProc`：對該資料夾呼叫跟監控啟動時初始掃描完全相同的 `LoadOrCreateLogFile` + `ScanForNewFiles`，把「歷史紀錄裡還沒有的檔案」附加進該資料夾自己的 `file_list.txt`。
  - **刻意不另外開新檔案**：一開始設計時有考慮輸出到獨立的清單檔（例如 `file_list_full.txt`），但使用者明確要求「就是跟 file_list.txt 混在一起」，所以改成直接重用 `ScanForNewFiles` 寫回同一份歷史紀錄。
  - 不需要「開始監控」正在執行也能用；跟即時監控背景執行緒是各自獨立的 `std::thread`（`g_exportThread`／`g_isExporting`），兩者可以同時運作，寫入 `file_list.txt` 的部分由既有的 `g_logMutex` 保護，不會互相干擾或造成重複紀錄。
  - 目前「匯出中」是**全域單一旗標**（`g_isExporting`），同一時間只能有一個匯出操作在跑，不區分是哪個資料夾（簡化設計，避免多個匯出執行緒同時搶用同一個按鈕的啟用/停用狀態造成混亂）；按鈕在掃描進行中會停用（`EnableWindow(g_hBtnExport, FALSE)`），完成後透過 `WM_APP_SCAN_DONE` 訊息通知 UI 執行緒重新啟用；掃描完成會在「監控紀錄」欄位顯示新增了幾筆紀錄。
- **`file_list.txt` 是永久累積的歷史紀錄，不會被清空／覆寫**：這是為了支援「重複檔案偵測」——如果每次啟動都清空重寫，就沒辦法知道某個檔案是不是很久以前記錄過。啟動時改成：檔案不存在才建立（寫入 BOM+表頭）；檔案已存在就讀取全部內容，把「相對路徑 -> 最後一次記錄時間」載入記憶體中的歷史表（`FileHistory`，`std::map`，key 用小寫化路徑比對，忽略大小寫）。初始掃描只會對「歷史表裡沒有的檔案」附加新紀錄，已經在歷史表裡的檔案（代表上次關閉程式前就已經在資料夾裡）直接略過，不會重複記錄，也不會跳出提示。
- **重複檔案偵測與批次提示**：即時監控迴圈中，同一次 `ReadDirectoryChangesW` 完成事件（同一個緩衝區）收到的所有「新檔案出現」通知，會先分類：
  - 相對路徑不在歷史表 → 直接呼叫 `AppendRecord` 記錄，和先前行為一樣。
  - 相對路徑已在歷史表 → 收集進 `duplicates` 陣列，等這批事件全部處理完，才呼叫 `HandleDuplicateFiles` 一次跳出視窗。
  - 這代表「批次處理」的範圍是**單次 `ReadDirectoryChangesW` 緩衝區**，不是全域去重；如果同一批大量檔案移入的通知被 Windows 拆成好幾次緩衝區完成，可能會連續跳出好幾個提示視窗（而不是完全合併成一個）。64KB 緩衝區通常足以裝下一次拖拉/貼上操作的所有檔案通知，一般使用情境下不會遇到這個限制。
  - 重複檔案清單視窗（`FileWatcherDupDlg`）：用 `SysListView32`（`LVS_EX_CHECKBOXES`）顯示每個重複檔案的相對路徑與上次記錄時間，附「全選(刪除)」「全不選(保留)」快速按鈕，以及「確定」套用。直接關閉視窗（右上角 X）視同「全部保留」，不會刪除任何檔案。
  - 這個視窗是**獨立的頂層視窗**（`WS_POPUP`，owner 設為主視窗），即使主視窗被隱藏在系統匣（開機自動背景執行模式）也一樣會顯示出來，並呼叫 `SetForegroundWindow` + `FlashWindow` 讓使用者注意到；顯示期間會 `EnableWindow(owner, FALSE)` 讓主視窗暫時不可互動（簡易 modal 效果）。
  - 這個視窗是在**背景監控執行緒**上建立、跑自己的訊息迴圈（不是 UI 執行緒），所以顯示期間監控迴圈會停在這裡等待使用者選擇，這是刻意的設計（讓「決定重複檔案怎麼處理」這件事優先於繼續監控）。
  - 使用者選擇後：勾選「刪除」的檔案會用 `fs::remove` 刪除，記錄一行「[已刪除重複檔案]」到畫面，**不會**寫入 `file_list.txt`；未勾選（保留）的檔案會重新讀取當下容量、呼叫 `AppendRecord` 新增一筆紀錄（時間是使用者確認的當下時間，不是最初移入的時間），並更新歷史表。處理前都會重新檢查檔案是否還存在，避免使用者猶豫期間檔案已被移走造成誤操作。

## 編譯方式
```
# MSVC（Visual Studio 開發人員命令提示字元）
cl /EHsc /std:c++17 FileWatcher.cpp /link /SUBSYSTEM:WINDOWS ole32.lib shell32.lib advapi32.lib comctl32.lib

# MinGW-w64（務必加上 -static 系列參數，見下方疑難排解紀錄；-mwindows 讓它是視窗子系統程式，不會跳出黑底主控台）
g++ -std=c++17 -O2 -municode -mwindows -static-libgcc -static-libstdc++ -static FileWatcher.cpp -o FileWatcher.exe -lole32 -lshell32 -ladvapi32 -lcomctl32
```

## 開發規則（Claude 必須遵守）
- **每次修改 `FileWatcher.cpp` 後，都要自動執行編譯，產生最新的 `FileWatcher.exe`，不用等使用者要求。**
  - 預設使用上面的 MinGW-w64 g++ 指令（含 `-static` 系列參數，確保雙擊可獨立執行、不需要 MSYS2 執行期 DLL）。
  - 注意：在 Claude 的 Bash 工具（Git Bash）環境裡，`g++` 不在 PATH 上，必須用完整路徑呼叫：`/c/msys64/mingw64/bin/g++.exe`（對應 Windows 路徑 `C:\msys64\mingw64\bin\g++.exe`）。
  - **光用完整路徑呼叫 g++ 還不夠**：g++ 實際呼叫的 `cc1plus.exe`（位於 `mingw64/lib/gcc/x86_64-w64-mingw32/<版本>/cc1plus.exe`）啟動時要載入 `libmpfr-6.dll`、`libmpc-3.dll`、`libgmp-10.dll` 等執行期 DLL，這些 DLL 放在 `mingw64/bin`，但 Git Bash 的 PATH 預設不含這個目錄，導致 `cc1plus.exe` 找不到 DLL 而編譯失敗（exit code 1，且有時會把 Bash 工具的 shell 狀態重置）。**必須在呼叫 g++ 前，把 `/c/msys64/mingw64/bin` 加進 PATH**，正確指令：
    ```bash
    cd /d/FileWatcher && PATH="/c/msys64/mingw64/bin:$PATH" /c/msys64/mingw64/bin/g++.exe -std=c++17 -O2 -municode -mwindows -static-libgcc -static-libstdc++ -static FileWatcher.cpp -o FileWatcher.exe -lole32 -lshell32 -ladvapi32 -lcomctl32
    ```
    （`-mwindows` 讓它是視窗子系統程式；`-lole32 -lshell32` 是資料夾選擇對話框需要；`-ladvapi32` 是開機自動啟動的登錄檔讀寫需要；`-lcomctl32` 是重複檔案清單視窗的 `SysListView32` 核取方塊控制項需要。）
  - 編譯完成後，回報編譯結果（成功／失敗、警告訊息）。
  - 若編譯失敗，先嘗試排查原因（可參考下方「疑難排解紀錄」），並回報給使用者，不要留下無法編譯的程式碼卻不說明。
  - 若當次工具鏈不可用（例如雲端環境沒有 Windows 標頭檔），需明確告知使用者「本次僅完成程式碼修改，尚未在本機環境驗證編譯」，待使用者在 Windows 機器上編譯確認。
- **處理過程中出現的英文內容，一律自動翻譯成繁體中文，讓使用者能直接閱讀。**
  - 包含但不限於：編譯器錯誤／警告訊息、終端機指令的輸出結果、工具訊息、英文技術文件內容、程式庫或套件的英文說明。
  - 回報時先附上翻譯後的繁體中文說明；若原始英文訊息很關鍵（例如錯誤代碼、DLL 檔名、函式名稱），可附註原文以利查證，但主要說明一律用繁體中文表達，不要整段貼英文原文了事。
  - 程式碼本身（變數名、函式名、註解等）不在此規則範圍內，除非使用者另外要求翻譯程式碼註解。
- **測試 `SysListView32`（`監控資料夾`清單、重複檔案清單）時，絕對不要用 PowerShell 跨行程 `SendMessage` 送 `LVM_SETITEMSTATE`／`LVM_INSERTITEM` 等需要傳遞結構指標的訊息去操作選取狀態。** 2026-08-19 實測發現這樣做會讓 `FileWatcher.exe` 在 `COMCTL32.dll` 內部發生存取違規（0xC0000005）直接當掉整個行程（用 Windows 事件記錄檔的「Application Error」條目確認過，同一個模組位移每次都一樣，是可重現的當機，不是偶發）。換用「真正的滑鼠座標點擊」（`SetCursorPos` + `mouse_event` 對準該控制項換算後的螢幕座標）測試同一個操作，行程完全正常、沒有任何問題——證實這是測試方法本身的缺陷（跨行程傳遞含指標的訊息結構風險很高，即使把 C# 端結構大小校正到跟 Windows SDK 版本相符也一樣會炸），不是 `FileWatcher.exe` 程式碼的臭蟲。以後測試任何 ListView 的選取/勾選操作，一律用滑鼠座標點擊模擬；`BM_CLICK`（按鈕）之類不含指標酬載的簡單訊息才可以放心跨行程送。

## 使用方式
**第一次使用**：雙擊 `FileWatcher.exe` 開啟視窗，清單裡預設已經有一筆（程式啟動時的工作目錄）：
1. 選取清單中的一列，按「開始/停止監控」開始監控它；也可以先按「新增資料夾...」加入更多資料夾，再各自選取、各自按「開始/停止監控」。
2. 開始監控後，程式會：把整份清單存進設定檔、註冊開機自動啟動、建立該資料夾的初始 `file_list.txt`、開始背景監控。新增檔案會即時顯示在下方「監控紀錄」欄位並寫入該資料夾自己的 `file_list.txt`。
3. 按視窗右上角 X 只是縮到系統匣（所有監控中的資料夾繼續在背景執行）；要完全結束程式，在系統匣圖示按右鍵選「結束程式」。

**之後每次開機**：不用做任何事，Windows 登入後會自動在背景啟動並開始監控清單裡的**所有**資料夾（不會跳出視窗，只在系統匣看到圖示）。

**要增減監控的資料夾**：雙擊系統匣圖示（或右鍵選「顯示視窗」）叫出主視窗；按「新增資料夾...」加入新的，或選取一列、確認狀態是「已停止」後按「移除資料夾」移除（監控中的資料夾要先按「開始/停止監控」停止才能移除）。清單有變動時設定檔會自動更新。

**手動掃描某個資料夾**（不需要先開始監控）：在清單中選取該資料夾，按「匯出檔案清單」，會立刻遞迴掃描該資料夾（含所有子目錄），把還沒記錄過的檔案直接寫入它自己的 `file_list.txt`。

**系統匣選單**：右鍵系統匣圖示可以「顯示視窗 / 開始監控全部 / 停止監控全部 / 結束程式」，不需要開啟視窗就能一次控制所有資料夾。

## 執行狀態
- 2026-08-17：已在使用者的 Windows 電腦（DESKTOP-NFNFKT6）用 MSYS2 MinGW64 的 g++ 編譯成功，執行後正確產生初始 `file_list.txt`（含 UTF-8 BOM、Tab 分隔、時間戳記皆正確）。
- 2026-08-18：已實測確認「移入新檔案」行為正常。測試方式：建立空的監控目錄，啟動 `FileWatcher.exe` 監控該目錄，接著從外部目錄用 `Move-Item` 把檔案移入監控目錄（含直接移入根目錄、移入子目錄兩種情況），皆在約 1 秒內即時附加正確的紀錄（檔名相對路徑、容量、時間戳記）到 `file_list.txt`。遞迴子目錄監控確認有效。
- 2026-08-18：已將主控台版本改寫為視窗化（GUI）版本，並實測驗證。因為 Claude 沒有被授權用電腦操作工具（computer-use）直接控制這個自建的 .exe（不在系統認識的已安裝應用程式清單中），改用 PowerShell + Win32 API（`GetDlgItem`/`SendMessage BM_CLICK`）模擬按鈕點擊，並用 `PrintWindow` 截圖確認畫面內容，測試結果：
  - 視窗啟動正常，路徑欄位正確帶入預設工作目錄。
  - 按「開始監控」：正確建立初始 `file_list.txt`，畫面「監控紀錄」欄位即時顯示「正在建立初始檔案清單...」「初始清單建立完成」「[監控中]...」等訊息，「瀏覽...」按鈕正確變灰（監控中不可更換路徑）。
  - 移動檔案進監控資料夾：`file_list.txt` 即時附加正確紀錄，畫面同步顯示「[新增] 檔案完整路徑」。
  - 按「停止監控」：正確停止背景執行緒、按鈕文字變回「開始監控」、「瀏覽...」按鈕重新啟用、畫面顯示「[已停止] 監控已結束。」。
  - 未實測項目：「瀏覽...」按鈕開啟的標準 Windows 資料夾選擇對話框（`SHBrowseForFolderW`）本身的操作流程未逐步自動化測試（它是系統標準對話框，風險較低），建議使用者實際點擊試一次確認符合預期。
- 2026-08-18：加入「開機自動背景執行」功能，並實測完整流程，測試方式同樣是用 PowerShell + Win32 API（`GetDlgItem`/`SendMessage`/`PostMessage`/`FindWindow`）模擬操作，測試完已清除所有測試產生的登錄檔、設定檔、測試資料夾，未在使用者電腦留下任何殘留。測試結果：
  - **第一次執行**（無設定檔）：視窗正常顯示，選好資料夾按「開始監控」後，正確建立 `filewatcher_config.txt`（內容為選定路徑）與登錄檔 `HKCU\...\Run\FileWatcher`（內容為程式完整路徑）。
  - **按 X 關閉視窗**：程式沒有結束，`Get-Process` 確認行程仍在（`Responding=True`），只是視窗隱藏；此時移入新檔案，`file_list.txt` 仍正確即時更新，證明背景監控真的持續運作。
  - **系統匣選單「結束程式」**（用 `WM_COMMAND` 模擬點擊）：正確完全結束程式（`Get-Process` 確認行程已消失）。
  - **模擬開機自動啟動**（設定檔已存在時重新啟動程式）：`Get-Process` 確認 `MainWindowHandle` 為 0（全程沒有顯示任何視窗），但 `file_list.txt` 時間戳有更新（證明背景自動重新掃描），且移入新檔案也被即時記錄（證明背景監控自動啟動成功）。
  - 未實測項目：系統匣圖示的滑鼠右鍵選單本身的彈出/點選視覺流程（`TrackPopupMenu`）未逐步自動化測試，改用直接送出對應的 `WM_COMMAND` 訊息驗證各選單項目背後的邏輯是否正確；建議使用者實際在系統匣點一次右鍵確認選單有正常跳出。
- 2026-08-18：使用者要求明確確認「移入子目錄／子子目錄也必須記錄」這項需求。這個行為從最早的主控台版本就有實作（`ReadDirectoryChangesW` 的 `bWatchSubtree` 參數設為 `TRUE`），但程式後來歷經 GUI 化、改用 Overlapped I/O、加開機自動啟動等大改寫，尚未針對目前這份程式碼重新驗證過多層子目錄的情況，因此針對**目前版本**重新實測：建立含兩層巢狀子目錄（`sub\subsub`）的監控資料夾，分別把檔案移入根目錄、`sub\`、`sub\subsub\` 三個層級，三筆紀錄都正確即時附加到 `file_list.txt`，相對路徑正確顯示為 `a_root.txt`、`sub\b_sub.txt`、`sub\subsub\c_subsub.txt`。確認此需求在目前版本已滿足，不需要修改程式碼。
- 2026-08-19：加入「重複檔案偵測」核心功能（詳見「已確認的需求 / 設計決策」），並實測完整流程，測試方式同樣是 PowerShell + Win32 API 模擬操作（含用 `EnumWindows` 找出重複檔案清單視窗、`GetDlgItem`/`SendMessage BM_CLICK` 操作它的按鈕、`PrintWindow` 截圖確認畫面），測試完已清除所有測試產生的檔案、設定檔、登錄檔，未留下殘留。測試結果：
  - **從未記錄過的檔案移入**：兩個新檔案直接記錄到 `file_list.txt`，全程沒有跳出任何視窗，符合「不必理會」的需求。
  - **已記錄過的檔案再次移入**（用同名檔案覆蓋移入模擬）：正確跳出「發現重複移入的檔案」視窗，`SysListView32` 正確列出檔名(相對路徑)與上次記錄時間，核取方塊預設未勾選。
  - **不勾選、直接確定（保留）**：檔案維持存在，`file_list.txt` 新增一筆帶有最新容量與時間的紀錄，舊紀錄仍保留不變（歷史紀錄呈現「附加」而非「覆蓋」）。
  - **點「全選(刪除)」再確定（刪除）**：檔案被實際刪除（`Test-Path` 確認為 `False`），`file_list.txt` 沒有新增任何紀錄。
  - **重新啟動監控（模擬程式重開/開機自動啟動）**：`file_list.txt` 內容與重啟前完全一致（沒有被清空重寫），且資料夾裡仍存在、已經記錄過的檔案（`dup1.txt`）在初始掃描時沒有被重複記錄、也沒有跳出提示視窗，證實「歷史紀錄跨重啟持久化」與「初始掃描不誤判既有檔案為重複」都正確運作。
  - 未實測項目：單一批次通知裡包含大量（例如數百個以上）重複檔案時的清單視窗顯示與操作流暢度未實測；一次拖入的檔案量大到需要被 Windows 拆成多次 `ReadDirectoryChangesW` 完成事件時，是否會連續跳出多個提示視窗（設計上預期會，見「已確認的需求 / 設計決策」的說明）也未實測，屬於少見的邊界情況，一般使用情境應不會遇到。
- 2026-08-19：使用者反映「程式中的字形都很醜」，原因是所有控制項都沒有明確指定字型，Win32 預設退回到很舊的點陣字型（見「已確認的需求 / 設計決策」）。已修正並實測：啟動程式截圖比對，文字從原本鋸齒明顯的點陣字，變成平滑反鋸齒、與 Windows 原生對話框一致的字型，主視窗與重複檔案清單視窗的控制項都已套用。
- 2026-08-19：加入「匯出檔案清單」按鈕（詳見「已確認的需求 / 設計決策」）。原本設計是輸出到獨立檔案，使用者中途指正「就是跟 file_list.txt 混再一起」，改成重用 `ScanForNewFiles` 寫回同一份歷史紀錄，未另外開新檔案。實測結果：
  - 建立含子目錄的測試資料夾（根目錄 1 個檔案 + `sub\` 底下 1 個檔案），**完全不按「開始監控」**、直接按「匯出檔案清單」：兩個檔案（含子目錄裡的）都正確遞迴掃描並寫入 `file_list.txt`，證實此功能不需要監控先啟動即可獨立使用。
  - 對同一資料夾再按一次「匯出檔案清單」（沒有新檔案）：`file_list.txt` 內容完全沒變，沒有產生重複紀錄。
  - 在「開始監控」進行中，另外移入一個新檔案（被即時監控記錄），再按「匯出檔案清單」：行程沒有當掉（`Get-Process` 確認 `Responding=True`），`file_list.txt` 裡該檔案只出現一次，證實兩個背景執行緒（監控執行緒與匯出執行緒）可以並存、不會互相干擾或造成重複紀錄。
- 2026-08-19：使用者詢問「可否監控多個目錄」，確認原設計是單一目錄後，依使用者要求改寫成**多目錄監控**版本（詳見「已確認的需求 / 設計決策」的多目錄架構說明）。測試過程中發現測試方法本身的一個嚴重陷阱並已記錄進「開發規則」（跨行程送 `LVM_SETITEMSTATE` 會讓程式在 `COMCTL32.dll` 內部當掉，換成真滑鼠座標點擊就完全正常，這是測試工具的問題不是程式的問題）。改用滑鼠點擊 + `BM_CLICK`（按鈕）+ 直接送 `WM_COMMAND`（系統匣選單項目、需要跳過選單視覺互動時）重新完整測試，結果：
  - **第一次執行**：清單預設有一筆（工作目錄），狀態「已停止」；新增/移除資料夾功能正常（用滑鼠點擊清單列選取，`BM_CLICK` 按鈕）。
  - **多資料夾各自獨立監控**：手動寫入含兩個資料夾路徑的多行設定檔後重啟，兩個資料夾**同時**被靜默背景啟動（`MainWindowHandle=0`，沒有跳出任何視窗），兩個資料夾的初始掃描與即時監控都正確運作，各自新增的檔案都正確記錄到各自的 `file_list.txt`，監控紀錄欄位裡兩個資料夾的訊息正確交錯顯示（用完整路徑分辨來源）。
  - **獨立啟停**：選取其中一個資料夾按「開始/停止監控」使其停止，之後只有它不再記錄新檔案，另一個資料夾完全不受影響、繼續正常監控——證實 `MonitorEntry` 之間確實互相獨立。
  - **系統匣「開始監控全部」**：對已停止的資料夾重新全部啟動，正確補上停止期間錯過的檔案（初始掃描會重新抓一次目前狀態）。
  - **系統匣「停止監控全部」**：正確把所有監控中的資料夾一次停止，清單狀態欄位同步更新為「已停止」。
  - **安全防呆**：對監控中的資料夾按「移除資料夾」，正確跳出「請先停止」提示視窗擋下操作，不會誤刪正在使用中的項目。
  - 測試完已清除所有測試資料夾、設定檔、登錄檔、暫存截圖，以及測試當機過程中產生的 crash dump 檔案，未在使用者電腦留下任何殘留。
  - 未實測項目：透過「新增資料夾...」跳出的標準資料夾選擇對話框本身的操作流程（原因同前，是系統標準對話框，風險較低，且無法安全用跨行程訊息模擬選取，故未逐步自動化）；建議使用者實際點擊試一次確認符合預期。單一批次通知裡包含大量重複檔案、或多個資料夾同時跳出重複檔案提示視窗時彼此的視窗焦點/modal 行為，也還沒有實測過。
- 2026-08-19：使用者說明了真實使用情境（見「真實使用情境」）——下載日文命名的檔案、移入監控資料夾後改成中文檔名，需要靠原始日文檔名判斷「是否已經處理過」。針對這個情境用真正的日文檔名（`テスト動画ファイル.mp4`，含平假名「テスト」、片假名「ファイル」、漢字「動画」）完整實測：
  - **移入日文檔名檔案**：`file_list.txt` 正確以 UTF-8 記錄完整日文檔名，沒有亂碼。
  - **在資料夾內把檔案改名成中文**（`測試影片檔案.mp4`）：改名本身觸發 `FILE_ACTION_RENAMED_NEW_NAME`，被當成「新檔案出現」處理，中文新檔名被記錄為新的一筆紀錄；**原本日文檔名那筆歷史紀錄完全沒有被覆蓋或移除**，兩筆並存。
  - **模擬又下載一次同名日文檔案並移入**：正確跳出「發現重複移入的檔案」視窗，`SysListView32` 正確顯示日文檔名「テスト動画ファイル.mp4」（截圖確認無亂碼），且「上次記錄時間」正確對應到最初移入的時間（不受中間改名影響）。
  - **選擇刪除該重複檔案**：檔案被正確刪除，`file_list.txt` 沒有新增任何紀錄。
  - 結論：目前的「重複檔案偵測」機制（以檔名比對歷史紀錄、改名不影響既有歷史）已經完整涵蓋使用者描述的真實需求，不需要額外修改程式碼；此測試主要是驗證「日文字元編碼在整個流程（Windows API → 記憶體字串 → 寫入檔案 → 讀回歷史表 → 顯示在對話框）中都正確無誤」。

## 疑難排解紀錄
### 問題：編譯後 .exe 悄悄消失／完全沒有錯誤訊息
- **現象**：`g++` 執行後沒有任何錯誤輸出，但輸出的 `.exe`（包含最簡單的 hello world 測試）完全沒有出現在資料夾裡，`$LASTEXITCODE` 有時顯示 1。一開始懷疑是防毒軟體攔截，但檢查 Windows 安全性「防護記錄」與「受控資料夾存取權」都沒有異常。
- **真正原因**：`C:\msys64\mingw64\bin` 底下缺少 `libmpc-3.dll`，導致實際執行編譯工作的 `cc1plus.exe` 一啟動就跳出 Windows 系統錯誤對話框（「找不到 libmpc-3.dll」），整個編譯行程卡住，因此看起來像是「無聲失敗」。
- **修復方式**：開啟「MSYS2 MinGW64」終端機（不是一般的 cmd/PowerShell），執行：
  ```
  pacman -Syu
  pacman -S mingw-w64-x86_64-gcc
  ```
  重新安裝/修復 GCC 工具鏈後，`libmpc-3.dll` 等相依 DLL 就會補齊，`cc1plus.exe` 恢復正常，編譯即可成功產生 .exe。
- **額外排查眉角**：
  - PowerShell 若偵測到螢幕助讀程式會停用 PSReadLine，導致貼上多行指令時換行失效、多行指令黏成一行執行，出現許多莫名其妙的錯誤，需要 `Import-Module PSReadLine` 重新啟用，或改成一行一行分開貼上。
  - `cmd.exe` 的 `cd` 指令不會自動切換磁碟機（跨磁碟機要用 `cd /D D:\路徑` 或先單獨打 `D:` 切換）。
  - 在 MSYS2 MinGW64 的 Bash 環境裡，Windows 路徑要用 `/d/資料夾` 這種寫法，且不能用 `dir`/`cd /D` 這類 cmd.exe 語法（要用 `ls`、`cd`）。

### 問題：雙擊 FileWatcher.exe 跳出「找不到 libgcc_s_seh-1.dll」
- **現象**：在 MSYS2 MinGW64 終端機裡編譯、執行都正常，但直接在檔案總管雙擊 `FileWatcher.exe` 卻跳出系統錯誤「找不到 libgcc_s_seh-1.dll」。
- **原因**：預設（動態連結）編譯出來的 exe 需要依賴 `C:\msys64\mingw64\bin` 底下的 `libgcc_s_seh-1.dll`、`libstdc++-6.dll`、`libwinpthread-1.dll` 這幾顆執行期 DLL。在 MSYS2 終端機裡因為 PATH 有包含該路徑所以能找到，但用檔案總管雙擊時用的是系統 PATH，沒有這個路徑，就會找不到。
- **修復方式**：編譯時加上靜態連結參數，把這些程式庫直接打包進 exe，之後就能在任何地方雙擊獨立執行，不再依賴 MSYS2 環境或需要修改系統 PATH：
  ```
  g++ -std=c++17 -O2 -municode -static-libgcc -static-libstdc++ -static FileWatcher.cpp -o FileWatcher.exe
  ```

## 待辦 / 尚未確認事項
- 是否需要排除特定副檔名或隱藏檔，尚未確認。
- 建議使用者實際操作一次「新增資料夾...」跳出的資料夾選擇對話框、以及系統匣圖示右鍵選單，確認符合預期（這兩處目前只驗證了背後邏輯，沒有逐步自動化模擬滑鼠點選視覺流程，詳見「執行狀態」）。
- 目前移除「開機自動啟動」沒有內建 UI（需手動刪登錄檔 + 設定檔，見「已確認的需求 / 設計決策」），是否需要在系統匣選單加一個「取消開機自動啟動」的選項，尚未確認。
- `file_list.txt` 現在會永久累積、不會自動清理，如果同一個資料夾長期使用（尤其常有重複檔案被保留並重新記錄），檔案可能會越長越大，是否需要加上「歷史紀錄上限」或「定期封存」機制，尚未確認。
- 重複檔案的判斷目前只比對「相對路徑」（忽略大小寫），不比對檔案內容或容量是否相同；如果需要「同名但內容不同」與「內容完全相同的重複」分開處理，需要另外討論比對方式（例如加入雜湊比對）。
- 「匯出檔案清單」的「匯出中」狀態目前是全域單一旗標，同一時間只能有一個資料夾在匯出（見「已確認的需求 / 設計決策」），如果使用者常需要同時對多個資料夾匯出，可能需要改成每個資料夾各自獨立的匯出狀態。
- 多個資料夾如果幾乎同時都跳出「重複檔案」提示視窗，目前每個視窗各自對主視窗做 `EnableWindow(FALSE/TRUE)`，理論上先完成的視窗關閉時可能會把主視窗提前解鎖、即使還有其他視窗在等待，這是已知的小瑕疵（機率低、影響輕微，僅為介面互動細節，未實測也未修正）。
